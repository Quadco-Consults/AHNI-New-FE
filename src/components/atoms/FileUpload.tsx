"use client";

import React from 'react';

const FileUpload = () => {
  return (
    <div>
      {/* File Upload component placeholder */}
      <input type="file" />
    </div>
  );
};

export default FileUpload;